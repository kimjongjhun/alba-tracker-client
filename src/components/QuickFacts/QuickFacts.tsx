const styles = {
  background: "pink",
  padding: "8px",
};

const QuickFacts = () => {
  return (
    <div style={styles}>
      <div>quick facts</div>
    </div>
  );
};

export default QuickFacts;
